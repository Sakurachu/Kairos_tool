# Public values are bundled into the browser and must not contain secrets.
NEXT_PUBLIC_SITE_NAME=小工具
NEXT_PUBLIC_SITE_DESCRIPTION=小工具测试
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Add the database connection after choosing Supabase or Neon.
DATABASE_URL=

# Generate a long random value before enabling admin actions.
ADMIN_SECRET=
